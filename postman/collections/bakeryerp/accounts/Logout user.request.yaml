$kind: http-request
url: "{{baseurl}}/account/logout/"
method: POST
body:
  type: json
  content: "{\r

    \  \"refresh\": \"{{refresh_token}}\"\r

    }"
auth:
  type: bearer
  credentials:
    token: "{{access_code}}"
order: 1000
